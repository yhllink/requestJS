import type { Method, RequestData, requestCommonReturn, Params } from '../index'

export default async function xhr(method: Method, url: string, data: RequestData, params: Params): Promise<requestCommonReturn> {}
